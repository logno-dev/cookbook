/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  // Note: Tailwind v4 handles dark mode differently, but this should work
  theme: {
    extend: {},
  },
};